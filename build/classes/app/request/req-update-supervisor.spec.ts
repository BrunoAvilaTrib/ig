import { ReqUpdateSupervisor } from './req-update-supervisor';

describe('ReqUpdateSupervisor', () => {
  it('should create an instance', () => {
    expect(new ReqUpdateSupervisor()).toBeTruthy();
  });
});
